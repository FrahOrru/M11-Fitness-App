query Programs {
  programs {
    id
    image {
      id
      url
    }
    name
    workouts {
      description
      id
      image {
        id
        url
      }
      name
      day
    }
    description
  }
}